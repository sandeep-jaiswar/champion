# Validation

Domain scope: data validation flows, rules, and schemas.

Current contents:

- Core validator and flows scaffold.

Migration notes:

- Move validation package from `validation/src/validation` here.
- Add reusable checks for lake/warehouse tables; keep schemas close to parsers.
