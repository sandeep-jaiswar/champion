apiVersion: v1
kind: Secret
metadata:
  name: champion-secrets
  namespace: champion
type: Opaque
stringData:
  # WARNING: Replace these default passwords with strong, randomly generated passwords
  # These are placeholders only. Never use default passwords in production!
  # Generate secure passwords using: openssl rand -base64 32
  CLICKHOUSE_USER: "champion_user"
  CLICKHOUSE_PASSWORD: "REPLACE_WITH_SECURE_PASSWORD"
  GF_SECURITY_ADMIN_PASSWORD: "REPLACE_WITH_SECURE_PASSWORD"
